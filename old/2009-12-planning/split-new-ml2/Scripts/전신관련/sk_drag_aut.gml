//드래그 안에있는지 확인함

if ((x>=argument0 && x<=argument2) || (x<=argument0 && x>=argument2)) && ((y>=argument1 && y<=argument3) || (y<=argument1 && y>=argument3))
{return true}
else
{return false}

// sk_drag_aut(드래그시작점x, 드래그시작점y, 드래그끝점x, 드래그끝점y)
