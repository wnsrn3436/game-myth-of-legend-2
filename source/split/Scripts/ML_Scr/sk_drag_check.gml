//드래그 안에있는지 확인함

if ((x>=argument0 && x<=argument2) || (x<=argument0 && x>=argument2)) && ((y>=argument1 && y<=argument3) || (y<=argument1 && y>=argument3)){return 1}
return 0

// sk_drag_check(x1, y1, x2, y2)
